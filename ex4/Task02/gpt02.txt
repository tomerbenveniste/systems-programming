מה זה תופעת מעבר?
Thought for a couple of seconds

תופעת מעבר היא השלב הקצר שבו מעגל חשמלי “מתארגן מחדש” אחרי שינוי פתאומי.

לדוגמה: כשמחברים מתח למעגל עם קבל, המתח על הקבל לא קופץ מיד לערך הסופי. הוא עולה בהדרגה:

V
C
	​

(t)=V
S
	​

(1−e
−t/RC
)

כלומר:

בהתחלה הקבל ריק, אז V
C
	​

=0
אחרי זמן מסוים הוא נטען
אחרי בערך 5τ אומרים שהוא כמעט טעון לגמרי
ואז המעגל נמצא ב־מצב יציב

הסימן:

τ=RC

נקרא קבוע הזמן של המעגל.

במעגל RC:

τ=R⋅C

אחרי 1τ, הקבל מגיע בערך ל־63% מהמתח הסופי.

אחרי 5τ, הוא מגיע בערך ל־99%, ולכן אומרים שהמעבר נגמר והמעגל במצב יציב.

במילים פשוטות:
תופעת מעבר היא הזמן שבו המתח/הזרם משתנים עד שהם מגיעים לערך הקבוע שלהם.

דוגמה מהחיים: כמו למלא כוס מים. בהתחלה הכוס ריקה, אחר כך המפלס עולה, ובסוף הוא מתייצב. התקופה שבה המפלס עולה היא “תופעת המעבר”.

how to show the volts?
Thought for a couple of seconds

You already are showing voltage — the green graph is voltage, and it says 1 V / 0 V.

To show the voltage value on the circuit itself:

Click the wire/node you want to measure
For example, click the top of the capacitor.

Look at the bottom/right info panel.
It should show something like:

V = ...

To make it clearer, add a voltage probe:
Right click on the wire/node
Choose View in Scope / Add Scope
Or use the probe/voltage measurement tool if available

For measuring capacitor voltage, measure between the two capacitor terminals:

Top terminal = capacitor voltage
Bottom terminal is connected to ground = 0 V

So the top node voltage is basically:

V
C
	​


In your screenshot, the capacitor is charging and discharging, so the voltage is the blue/purple curve or the selected node curve depending on what you clicked. The green square wave is the input source.

למה הקווים לא ישקרים?
Thought for a second

Yes, but in C it is not as clean as in higher-level languages.

Because these two lists have different types:

ptr *head      // item list
wlst *head     // warehouse list

and different fields:

head->data->id
head->data->code

So the simple answer: you can unite the logic, but not directly with one normal function unless you use void*, function pointers, or macros.

Best simple option: keep them separate

For your level / assignment, this is probably best:

int search_item_by_id(ptr *head, int id) {
    if (!head) {
        return -1;
    }

    if (head->data->id == id) {
        return 1;
    }

    return search_item_by_id(head->next, id);
}
int search_warehouse_by_code(wlst *head, int code) {
    if (!head) {
        return -1;
    }

    if (head->data->code == code) {
        return 1;
    }

    return search_warehouse_by_code(head->next, code);
}

Also notice: in your first function you wrote:

return search_item_by_id(head->next, id);

but the function name is:

search_by_id

So either rename the function or fix the recursive call.

If you really want one function

You can use a macro:

#define SEARCH_LIST(head, value, field) \
    ((head) == NULL ? -1 : \
    ((head)->data->field == (value) ? 1 : SEARCH_LIST((head)->next, (value), field)))

Then:

int found_item = SEARCH_LIST(item_head, id, id);
int found_warehouse = SEARCH_LIST(warehouse_head, code, code);

But macros can be confusing and harder to debug.

Practical recommendation

For now, keep two functions.
They are short, clear, and type-safe.

Just maybe rename them clearly:

int search_item_by_id(ptr *head, int id);
int search_warehouse_by_code(wlst *head, int code);

That is the cleanest solution for this assignment.

realloc what is it?
Thought for a second

realloc משנה גודל של זיכרון שכבר הקצית עם malloc.

דוגמה בסיסית
int *arr = malloc(5 * sizeof(int));

פה יצרת מקום ל־5 מספרים.

עכשיו אתה רוצה להגדיל ל־10:

arr = realloc(arr, 10 * sizeof(int));

עכשיו arr מצביע למקום שיש בו מקום ל־10 מספרים.

למה צריך realloc?

כשאתה לא יודע מראש כמה איברים יהיו.

לדוגמה:
בהתחלה אתה מקצה מקום ל־5 מוצרים, ואז המשתמש מוסיף עוד, אז אתה מגדיל את המערך.

חשוב מאוד

לא לכתוב ישר ככה בלי בדיקה:

arr = realloc(arr, new_size);

כי אם realloc נכשל, הוא מחזיר NULL, ואז איבדת את הכתובת הישנה.

עדיף:

int *temp = realloc(arr, 10 * sizeof(int));

if (temp == NULL) {
    print_error_message(1);
}

arr = temp;
מה realloc עושה בפועל?

יש שתי אפשרויות:

אפשרות 1: יש מקום להרחיב במקום
arr ---> [old memory expanded]

הכתובת נשארת אותה כתובת.

אפשרות 2: אין מקום ליד הזיכרון הישן

אז realloc:

מקצה מקום חדש
מעתיק את המידע הישן לשם
משחרר את המקום הישן
מחזיר כתובת חדשה
old arr ---> [old memory]

new arr ---> [new bigger memory with copied data]
כלל קצר
פונקציה	מה עושה
malloc	מקצה זיכרון חדש
calloc	מקצה זיכרון חדש ומאפס אותו
realloc	משנה גודל של זיכרון קיים
free	משחרר זיכרון

דוגמה מלאה:

int capacity = 5;
int *arr = malloc(capacity * sizeof(int));

capacity = 10;
int *temp = realloc(arr, capacity * sizeof(int));

if (temp == NULL) {
    free(arr);
    print_error_message(1);
}

arr = temp;

realloc = “תן לי את אותו מערך, אבל בגודל חדש”.

main_template.c:76:16: warning: returning ‘itemlst *’ from a function with incompatible return type ‘item *’ [-Wincompatible-pointer-types]
   76 |         return head;
      |           

ה־warning אומר:

הפונקציה שלך מוגדרת כמחזירה:

item *

אבל בשורה הזאת:

return head;

head הוא:

itemlst *

כלומר אתה מחזיר סוג מצביע לא נכון.

דוגמה למה שכנראה יש לך
item* create_item(...) {
    itemlst *head;
    ...
    return head;   // wrong
}

אסור, כי:

item*   !=   itemlst*
אם זו פונקציה create_item

היא צריכה להחזיר את ה־item שיצרת:

return itm;

לא:

return head;

כלומר:

item* create_item(...) {
    item *itm = malloc(sizeof(item));
    ...
    return itm;
}
אם זו פונקציה insert_item

אז ההגדרה שלה צריכה להיות:

itemlst* insert_item(itemlst *head, item *itm)

ובתוכה כן אפשר:

return head;

או:

return new_node;

כי אז הפונקציה באמת מחזירה itemlst *.

השורה התחתונה:

return head;

מתאים רק לפונקציה שמחזירה:

itemlst *

בתוך create_item תחזיר:

return itm;